import '../state/check_in_cubit.dart';

/// Release/profile stub — no demo strings or random data in the binary.
abstract final class CheckInDemoDefaults {
  static bool get enabled => false;

  static CheckInState initial() => const CheckInState();
}
